server:
  port: 8080
  servlet:
    context-path: /api
logging:
  level:
    root: info
    com.springboot: info
  file:
    name: logs/my.log

    # 開發環境
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/charlie
    username: root
    password: qweasdzxc123+*
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

